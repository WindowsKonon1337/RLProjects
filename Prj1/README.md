# How to visualize grpaph?

basic command looks like:

```python
python Prj1/visualize_room_graph.py --save graph.png
```

additinal parameters you can find via ```--help``` argument or in the bottom of [this script](./visualize_room_graph.py)
