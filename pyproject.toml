[project]
name = "rl2026"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "matplotlib",
    "torch",
    "scikit-learn",
    "plotly",
    "pandas>=3.0.0",
    "seaborn>=0.13.2",
]

[tool.uv]
dev-dependencies = []
