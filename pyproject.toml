[project]
name = "rl2026"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "matplotlib",
    "torch",
    "scikit-learn",
    "plotly",
]

[tool.uv]
dev-dependencies = []
